export default class ArticleDetail{
	constructor({browser_num, comment_num, content, coll_num, md_content,
		modify_time, nick_name, tech_type, title, topic_id, type,user_id})
	{
		this.browser_num = browser_num
		this.comment_num = comment_num
		this.content = content
}
}